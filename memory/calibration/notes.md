# Calibration notes

Dated observations from /calibrate about systematic over/under-confidence.
One line each: date, category, claimed vs. actual, corrective rule adopted.

- 2026-06-17 · buckets low+mid (general) · UNDERCONFIDENT, n=75 b=0.159: low<0.6 claimed 53% / actual 76%; mid 0.6-0.85 claimed 69% / actual 89%. No overconfident bucket. Rule: when I've actually verified the evidence (checked an artifact/transcript, not guessed), state the confidence the evidence warrants instead of reflexively hedging to 0.6-0.7 — bump stated confidence ~15-20pts toward demonstrated accuracy. Reserve sub-0.7 for genuinely unreconstructable / user-taste-dependent outcomes (e.g. "user adopts a divergence" — see 54d8ea10 miss).
- 2026-06-21 · design+build self-assessment · OVERCONFIDENT on self-belief, n=2 hit 0%: 8cead3e1 (design, claimed 0.6) + 34b5ab6f (build, claimed 0.75) — both misses traced to over-trusting my own unverified spec/tests. Distinct axis from the 2026-06-17 underconfidence note: that one is hedging on VERIFIED external evidence; this is UNVERIFIED self-belief. Rule: an `--expect` clause asserting (a) "no sibling/existing system already does X" or (b) "my own spec/tests/artifact is correct" is a claim to FALSIFY, not evidence — keep such clauses ≤0.5 until grounded by an independent check (read the sibling's actual artifacts; run a fresh-context critic on your own tests before trusting them). No conflict with 2026-06-17: bump confidence AFTER you verify, not before.
- 2026-06-21b · self-belief predictions (follow-up to 2026-06-21), n=3 · Two anti-self-belief predictions this window HIT under the ≤0.5 cap (3bb01a8a design, 42dba6d9 build) while the `low<0.6` bucket ran claimed 52% / actual 78% — the blanket cap can be too blunt. Two sharpenings: (1) the cap is for UNVERIFIED self-belief ONLY — a clause grounded in INDEPENDENT recon AT PREDICT-TIME (e.g. a multi-agent map cross-checked against the actual source line) is already-checked evidence; split a bundled `--expect` and let the recon-grounded clause carry the confidence its evidence warrants, reserving ≤0.5 for the genuinely-unchecked clause (the critic/auditor has not run yet). (2) When predicting your OWN build, do NOT predict "it is correct" (the clause to falsify) — predict the FIX-CYCLE ("first pass needs ≥1 bug caught by RED-first + fresh-context critic"); that is a process claim robust to the bug, and it broke the prior build-MISS streak (8cead3e1, 34b5ab6f). Unchanged: an ASSERTED recon is not a PERFORMED one — bump only after the cited check exists.
- 2026-06-21c · build predictions (follow-up to 2026-06-21b), n=3 all HIT · 2026-06-21b rule (2)'s fix-cycle hedge now OVER-fires: all 3 SDD builds (280ecb8b/0b08d80c/3326a60a) predicted "first pass needs ≥1 bug / a fix-cycle" yet each went green FIRST try, keeping `low<0.6` underconfident (claimed 53% / actual 80%, n=20). Sharpening: the fix-cycle hedge is right for an UNVERIFIED build, but when the cross-test interactions AND fixture fingerprints are MAPPED PRE-BUILD (Phase B caught in DESIGN — before any run — that it would flip test_spec.py group-2b's "Phase A adds NO warning" assertion; Phase C pre-verified the fingerprint strings against the impl), the fix-cycle is pre-paid in recon and will NOT fire — drop that clause toward ~0 and let the independently-verifiable trunk-neutrality clause (`--check` clean, baseline.json byte-identical) carry the confidence. Reserve the fix-cycle hedge for builds whose cross-test/fixture interactions were NOT pre-mapped. No conflict with 2026-06-21b: a PERFORMED pre-build cross-test map is checked evidence; an ASSUMED green is not.
